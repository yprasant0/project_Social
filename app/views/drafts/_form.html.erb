<%= form_with(model: draft, local: true) do |form| %>
    <% if draft.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(draft.errors.count, "error") %> prohibited this draft from being saved:</h2>
  
        <ul>
          <% draft.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>
  
    <div class="field">
      <%= form.label :attachment %>
      <%= form.text_field :attachment %>
    </div>
  
    <div class="field">
      <%= form.label :content %>
      <%= form.text_area :content %>
    </div>
  
  
    <div class="actions">
    <span><%= form.submit "published", :name => "submit" %>
    <%= form.submit "draft", :name => "submit" %></span>
    </div>
  <% end %>
  